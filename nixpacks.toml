[phases.setup]
nixPkgs = [
  "python311",
  "python311Packages.pip",
  "python311Packages.virtualenv",
  "gcc",
  "libffi",
  "pkg-config",
  "cairo",
  "pango",
  "gdk-pixbuf",
  "glib",          # Keep this, it's the core GLib
  "gtk3",          # Keep this, it pulls in many related libs
  "gobject"        # Explicitly add gobject
]

[phases.install]
cmds = [
  "python3.11 -m venv /app/venv",
  ". /app/venv/bin/activate && pip install --upgrade pip",
  ". /app/venv/bin/activate && pip install -r requirements.txt"
]

[start]
cmd = "/app/venv/bin/gunicorn tile_estimator.wsgi:application"